{% extends 'base.html.twig' %}
{% block title %}Page d'acceuil{% endblock %}
{% block body %}
    {# Header #}
    <section class="header animate__animated animate__fadeInLeft">
        <img class="profil_picture" src='{{ asset('/img/profil.jpg') }}'/>
            <span>
                <hr class='hr_title'>
            </span>
        <div >
            <h1 >Dimitri Klopfstein</h1>
            <h3>développeur web et mobile</h2>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer ac tempor nibh. Sed et elementum mi. Etiam mattis faucibus mi et consequat. Vivamus at elementum diam. Nunc dignissim sodales purus ut egestas. Cras pulvinar mauris nec nisi pulvinar, et laoreet ligula cursus. Proin quis nulla ut leo consectetur posuere. Maecenas ac consequat sapien, quis rhoncus ipsum. Vivamus mattis iaculis volutpat. Duis euismod pharetra felis, id finibus arcu tempus vel. Nam vel semper libero. Pellentesque consequat viverra velit, vitae congue odio consectetur quis. Nunc venenatis neque nec leo laoreet semper. Phasellus rhoncus quam nec ex aliquam laoreet. Integer at diam bibendum, laoreet eros sed, consectetur lectus.</p>
        </div>
    </section>

    <div class="view_project">
        <span>voir les projets</span>
        <img  src='{{ asset('/img/icon-arrow.png') }}'/>
    </div>

   {# Aperçu projets#}
    <section>   
        <div class="titles">
            <hr class="project"/>
            <div class="title_h2">
                <h2>Work work work ...</h2>
                <span>Work work work ...</span>
            </div>       
        </div>
        <div class="projets_all">
  
            {# <div class="box"> #}
                {# <div class='box1'> #}
                {# {% for project in projects %} #}
                    {# {% if loop.first %} #}
                    {# <div class="projet projet1 image"> #}
                    {# <img src="{{ vich_uploader_asset(project, 'imageFile') }}" alt=""> #}
                        {# <div class="image__title">                       <p>ProjetName</p> #}
                            {# <p class="projet_name">{{ project.name | u.truncate(20, '...') }}</p> #}
                        {# </div> #}
                    {# </div> #}
                    {# {% elseif loop.last %} #}
                    {# <div class="projet projet2 image"> #}
                        {# <img src="{{ vich_uploader_asset(project, 'imageFile') }}" alt=""> #}
                        {# <div class="image__title"> #}
                            {# <p class="projet_name">{{ project.name | u.truncate(20, '...') }}</p> #}
                        {# </div> #}
                    {# </div> #}
                {# </div> #}
                {# <div class='box2'> #}
                {# {% else %}  #}
                    {# <div class="all_project"> #}
                        {# <p>En voir plus </p> #}
                    {# </div> #}
                    {# <div class="projet projet3 image"> #}
                        {# <img src="{{ vich_uploader_asset(project, 'imageFile') }}" alt=""> #}
                        {# <div class="image__title"> #}
                            {# <p class="projet_name">{{ project.name | u.truncate(20, '...') }}</p> #}
                        {# </div> #}
                {# </div> #}
        {# {% endfor %} #}
      {# </div>   #}
    
        
        {# <div class="box">        
            {% for project in projects %}
                {% if loop.first %}
                <div class="box1">
                    <div class="projet projet1 image">    
                    {% elseif loop.last %}
                    <div class="projet projet2 image">   
                </div>       
                {% else %}
                    <div class="box2">
                        <div class="all_project">
                            <p>En voir plus </p>
                        </div>
                            <div class="projet projet3 image">        
                            {% endif %}
                                <img src="{{ vich_uploader_asset(project, 'imageFile') }}" alt="">
                                <div class="image__title">
                                    <p class="projet_name">{{ project.name | u.truncate(20, '...') }}</p>
                                    {# <p class="projet_description">{{ project.description | u.truncate(110, '...') }}</p> #}
                                    {# <a class="projet_linkPage" href="{{ path('displayProject',{'id':project.id}) }}" >En savoir plus </a>
                                </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
           #} 
  
</section>

{# Invitation page Contact #}      
<section>   
    <div class="titles">
        <hr class=""/>
        <div class="title_h2">
            <h2>Vous en voulez plus?</h2>
            <span>Vous en voulez plus?</span>
        </div>       
    </div>
    <p class="">Ceci n'est que le début, j'ai encore pleins de chose à montrer
    .. T"est aller voir ce qui se cache <a href="/project">ici ?</a></p>
</section>
{% endblock %}



